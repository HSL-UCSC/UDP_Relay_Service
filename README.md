# unitree_matlab

sudo apt-get update  
sudo apt-get install libboost-all-dev cmake g++  

git clone https://github.com/lyphix/unitree_matlab.git  
cd unitree_matlab/  
mkdir build  
cd build/  
cmake ..  
make
